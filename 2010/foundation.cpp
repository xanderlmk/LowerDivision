
// Xander REYES
// ilab1
// 9/2/2021

#include <iostream>

using namespace std;


int main()
{
    // DECLARING AND INITIALIZING VARIABLES
    // ------------------------------------

    // declare a string variable called animal
    string animal;

    // declare 3 strings called first, last, middle, all on the same line
    string first, last, middle;

    // declare and initialize a string called 'state' with a value of California
    string state = "California";

    // declare an integer variable called population
    int population;
    
    // declare 2 integers called count and total, all on the same line
    int count, total;

    // declare and initialize an integer called grand_total. Set it to 200.
    int grand_total = 200;

    // declare a char variable called gender
    char gender;

    // declare 2 char variables called account_type and status, all on the same line
    char account_type, status;

    // declare and initialize a char variable called login_status. Set it to x
    char login_status = 'x';
   
    // declare a double variable called interest_rate


    // declare 3 double variables called rate1, rate2 and rate3, all on the same line


    // declare and initialize a double variable called percentage, all on the same line. Set it to 49.95
    double percentage = 49.95;

    // SETTING VALUES TO VARIABLES
    // ---------------------------


    // set the animal variable to an animal of your choosing
    animal = "allosaurus";

    // on 3 separate lines, set first middle and last to the 3 parts of the name "Bryce Dallas Howard"
    first = "Bryce";
    middle = "Dallas";
    last = "Howard";

    // set the population variable to 100000
    population = 100000;
    
    // set count to 0
    
    
    // set total to count (another way of saying "Assign count to total")


    // set char variable, gender to f
    gender = 'f';

    // on 2 separate lines, set account_type and status to c and o, respectively


    // set double interest_rate to 19.99


    // set rate1 to 0.15 and rate2 to three times the value of rate1
    double rate1 = 0.15;
    double rate2 = rate1 * 3;

    // set rate3 to the average of rate1 and rate2
    double rate3 = (rate1 + rate2) / 2;


    // DISPLAYING VALUES AND TEXT
    // --------------------------
    // For the tasks below, unless otherwise stated display everything on one line

    // display the value of animal 
    cout << animal << " ";

    // write a display statement so that the output is "Animal is porcupine" (where porcupine is 
    // the sample value in the animal variable

    
    // Do the same thing with the state variable


    // Display the full name comprised of the three parts (first, middle, last). Make sure to include spaces
    cout << first << " " << middle << " " << last << endl;

    // Display a blank line


    // Write display statements that looks like below
   // Twice the population is 200000    
    // Three times the population is 300000
    // DO NOT hard-code those numbers. Use the population variable


    // Display the value of count


    // Set the value of count to population


    // Display the value of count again


    // Set total to count


    // Display the value of total
    

    // Display 2 blank lines


    // display the char variables gender, account_type, status, and login_status all on one line
    // separate the characters with a dash character


    // Display a blank line


    // display interest_rate so that it shows like an actual percentage value with the % symbol
    

    // display rate1, rate2 and rate3 all on three separate lines


    // display the sum of rate1, rate2 and rate3 with the text "Sum of rates" preceding the sum value
    // perform the sum calculation of rate1, rate2 and rate3 as an inline expression


    // display the percentage variable and its value so that it looks like "Percentage = 49.95%"



    return 0;
}
